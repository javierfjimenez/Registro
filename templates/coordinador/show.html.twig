{% extends 'base.html.twig' %}

{% block title %}Coordinador{% endblock %}

{% block body %}
    <h1>Coordinador</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordinador.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ coordinador.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ coordinador.apellido }}</td>
            </tr>
            <tr>
                <th>Cedula</th>
                <td>{{ coordinador.cedula }}</td>
            </tr>
            <tr>
                <th>Mesa_electoral</th>
                <td>{{ coordinador.mesaElectoral }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ coordinador.telefono }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ coordinador.correo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coordinador_index') }}">back to list</a>

    <a href="{{ path('coordinador_edit', {'id': coordinador.id}) }}">edit</a>

    {{ include('coordinador/_delete_form.html.twig') }}
{% endblock %}
