{% extends 'base.html.twig' %}

{% block title %}Persona{% endblock %}

{% block body %}
    <h1>Persona</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ persona.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ persona.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido</th>
                <td>{{ persona.apellido }}</td>
            </tr>
            <tr>
                <th>Mesa_electoral</th>
                <td>{{ persona.mesaElectoral }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ persona.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ persona.telefono }}</td>
            </tr>
            <tr>
                <th>Cedula</th>
                <td>{{ persona.cedula }}</td>
            </tr>
            <tr>
                <th>Correo</th>
                <td>{{ persona.correo }}</td>
            </tr>
            <tr>
                <th>Persona_id</th>
                <td>{{ persona.personaId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('persona_index') }}">back to list</a>

    <a href="{{ path('persona_edit', {'id': persona.id}) }}">edit</a>

    {{ include('persona/_delete_form.html.twig') }}
{% endblock %}
